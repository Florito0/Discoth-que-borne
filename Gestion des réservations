<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gestion des Réservations</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Roboto', sans-serif;
        }
        
        body {
            background-color: #121212;
            color: #ffffff;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 20px;
        }
        
        .container {
            background-color: #1e1e1e;
            border-radius: 12px;
            width: 100%;
            max-width: 1000px;
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
        }
        
        .header {
            background-color: #2c2c2c;
            padding: 20px;
            text-align: center;
            border-bottom: 1px solid #333;
        }
        
        h1 {
            font-size: 24px;
            font-weight: 700;
            margin-bottom: 8px;
        }
        
        .subtitle {
            font-size: 16px;
            color: #b3b3b3;
        }
        
        .reservation-form {
            padding: 20px;
            display: flex;
            flex-direction: column;
            gap: 20px;
        }
        
        .form-group {
            display: flex;
            flex-direction: column;
            gap: 8px;
        }
        
        label {
            font-size: 16px;
            font-weight: 500;
        }
        
        input[type="text"],
        input[type="number"],
        input[type="date"],
        input[type="time"] {
            padding: 10px;
            background-color: #333;
            border: none;
            color: white;
            border-radius: 4px;
            font-size: 14px;
            width: 100%;
            max-width: 300px;
        }
        
        .date-time-group {
            display: flex;
            flex-direction: column;
            gap: 10px;
        }
        
        .date-time-row {
            display: flex;
            gap: 15px;
            align-items: center;
        }
        
        .separator {
            font-size: 16px;
            color: #b3b3b3;
            margin: 5px 0;
        }
        
        .error {
            color: #ff4444;
            font-size: 12px;
            display: none;
            margin-top: 5px;
        }
        
        .save-btn {
            padding: 15px;
            background: linear-gradient(to right, #9733EE, #DA22FF);
            color: white;
            border: none;
            border-radius: 6px;
            font-size: 18px;
            font-weight: 600;
            cursor: pointer;
            transition: opacity 0.2s;
            margin-top: 20px;
            width: 100%;
            max-width: 300px;
        }
        
        .save-btn:hover {
            opacity: 0.9;
        }
        
        @media (max-width: 768px) {
            .date-time-row {
                flex-direction: column;
                align-items: flex-start;
            }
            input[type="text"],
            input[type="number"],
            input[type="date"],
            input[type="time"] {
                max-width: 100%;
            }
            .save-btn {
                max-width: 100%;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Gestion des Réservations</h1>
            <p class="subtitle">Création et attribution des codes de réservation</p>
        </div>
        
        <div class="reservation-form">
            <div class="form-group">
                <label for="reservation-code">Code de réservation</label>
                <input type="text" id="reservation-code" placeholder="Ex: 001478" pattern="[A-Za-z0-9]{6}" required>
                <span class="error" id="code-error">Le code doit être alphanumérique (6 caractères)</span>
            </div>

            <div class="form-group">
                <label for="places">Nombre de places</label>
                <input type="number" id="places" min="1" placeholder="Ex: 5" required>
                <span class="error" id="places-error">Veuillez entrer un nombre valide (minimum 1)</span>
            </div>
            
            <div class="form-group">
                <label for="client-name">Nom du client</label>
                <input type="text" id="client-name" placeholder="Nom du client" required>
                <span class="error" id="name-error">Veuillez entrer un nom valide</span>
            </div>
            
            <div class="form-group">
                <label>Validité de la réservation</label>
                <div class="date-time-group">
                    <div class="date-time-row">
                        <input type="date" id="start-date" required>
                        <input type="time" id="start-time" required>
                    </div>
                    <span class="separator">à</span>
                    <div class="date-time-row">
                        <input type="date" id="end-date" required>
                        <input type="time" id="end-time" required>
                    </div>
                </div>
                <span class="error" id="date-error">Les dates et heures doivent être valides</span>
            </div>
            
            <button class="save-btn" onclick="saveReservation()">Enregistrer la réservation</button>
        </div>
    </div>

    <script>
        function saveReservation() {
            const code = document.getElementById('reservation-code').value;
            const places = document.getElementById('places').value;
            const name = document.getElementById('client-name').value;
            const startDate = document.getElementById('start-date').value;
            const startTime = document.getElementById('start-time').value;
            const endDate = document.getElementById('end-date').value;
            const endTime = document.getElementById('end-time').value;

            // Validation du code (6 caractères alphanumériques)
            const codeRegex = /^[A-Za-z0-9]{6}$/;
            if (!codeRegex.test(code)) {
                document.getElementById('code-error').style.display = 'block';
                return;
            } else {
                document.getElementById('code-error').style.display = 'none';
            }

            // Validation du nombre de places
            if (places < 1 || isNaN(places)) {
                document.getElementById('places-error').style.display = 'block';
                return;
            } else {
                document.getElementById('places-error').style.display = 'none';
            }

            // Validation du nom
            if (name.trim() === '') {
                document.getElementById('name-error').style.display = 'block';
                return;
            } else {
                document.getElementById('name-error').style.display = 'none';
            }

            // Validation des dates et heures
            const startDateTime = new Date(`${startDate}T${startTime}`);
            const endDateTime = new Date(`${endDate}T${endTime}`);
            if (startDateTime >= endDateTime) {
                document.getElementById('date-error').style.display = 'block';
                return;
            } else {
                document.getElementById('date-error').style.display = 'none';
            }

            // Si tout est valide, simuler l'enregistrement
            console.log({
                code,
                places,
                name,
                start: `${startDate} ${startTime}`,
                end: `${endDate} ${endTime}`
            });
            alert('Réservation enregistrée avec succès !');
        }
    </script>
</body>
</html>
